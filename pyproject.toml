[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "raspy"
version = "0.2.0"
description = "raspy — Raster Preparation System"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
